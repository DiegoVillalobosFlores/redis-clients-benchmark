import './globals.css'
import { Inter } from 'next/font/google'
import Link from "next/link";

const inter = Inter({ subsets: ['latin'] })

export const metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
      <header>
        <Link href={'/pages/static'}>Static page with IOredis: /pages/static</Link>
        <Link href={'/pages/dynamic'}>Dynamic page with IOredis: /pages/dynamic</Link>
        <Link href={'/routes/redis/dynamic'}>Node Redis route handler dynamic: /routes/redis/dynamic</Link>
        <Link href={'/routes/redis/static'}>Node Redis route handler static: /routes/redis/static</Link>
        <Link href={'/routes/ioredis/dynamic'}>IOredis route handler dynamic: /routes/ioredis/dynamic</Link>
        <Link href={'/routes/ioredis/static'}>IOredis route handler: /routes/ioredis/static</Link>
      </header>
      {children}
      </body>
    </html>
  )
}
